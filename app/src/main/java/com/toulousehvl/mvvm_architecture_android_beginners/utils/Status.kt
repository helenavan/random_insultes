package com.toulousehvl.mvvm_architecture_android_beginners.utils

/**
 * We need the enum to represent the UI State. We will create that in the utils package.
 * */

enum class Status {
    SUCCESS,
    ERROR,
    LOADING
}